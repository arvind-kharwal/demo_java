class Rectangle{
    int width, length; // Instance Variables
    void getdata(int width,int length){
        this.width = width;
        this.length = length;
    }
    int area(){
        int a = length*width;
        return(a);
    }

}
